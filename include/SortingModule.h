#ifndef SORTING_MODULE_H
#define SORTING_MODULE_H

#include "Scenario.h"
#include "StrategyResult.h"

// This runs the sorting-based planner on one scenario
StrategyResult runSortingStrategy(const Scenario& scenario);

#endif
